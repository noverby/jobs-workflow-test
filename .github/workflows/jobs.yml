name: "Trigger jobs"
on: 
  workflow_dispatch: 
    inputs: 
      jobs: 
        description: Jobs
        default: "jobs: {steps: ~}"
        required: true
? "${{ github.event.inputs.jobs }}"
: 
  job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: echo "Hello World!"
